# Lightweight dependencies for cloud deployment (Render/Cloud Run).
# The app detects the cloud env and runs MinimalConductor, which only needs
# a web server plus the provider SDKs. Heavy local-only deps (chromadb,
# sentence-transformers, langchain) are intentionally excluded for fast,
# reliable builds. For full local mode use requirements.txt instead.
fastapi>=0.109.0
uvicorn>=0.27.0
gunicorn>=21.2.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
jinja2>=3.1.3
aiofiles>=23.2.0
httpx>=0.26.0
requests>=2.31.0
openai>=1.10.0
anthropic>=0.18.0
google-generativeai>=0.3.0
# rich is imported unconditionally by utils/logger.py at module load —
# omitting it crashes the container before any lazy path can run.
rich>=13.7.0
